# Fixtures

Only synthetic or explicitly licensed test data belongs here.

Never commit real private meeting audio/transcripts.

Planned fixture groups:

- `audio/` deterministic PCM samples;
- `transcripts/` normalized partial/final events;
- `meetings/` synthetic multi-speaker evaluation sessions.
